<% if !(current_user?(@user)) && !old_event?(@event) %>
  <div id="attend_form">
  <% if current_user.attending?(@event) %>
    <%= render 'unattend' %>
  <% else %>
    <%= render 'attend' %>
  <% end %>
  </div>
<% end %>