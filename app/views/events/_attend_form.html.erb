<% unless current_user?(@user) %>
  <div id="attend_form">
  <% if current_user.attending?(@event) %>
    <%= render 'unattend' %>
  <% else %>
    <%= render 'attend' %>
  <% end %>
  </div>
<% end %>
